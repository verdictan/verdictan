pack:
  name: agent-context-fabric-invalid-conflict-resolution-strategy
  version: "1.0.0"
  enabled: true
policies:
  chain:
    - prompt-injection
agents:
  - id: code-assistant
    team: backend-eng
    context_fabric:
      conflict_resolution_strategy: LastWriteWins
